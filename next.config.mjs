/** @type {import('next').NextConfig} */
const nextConfig = {};

export default {
    nextConfig,
    images: {
        domains: ['financialmodelingprep.com'], // Add your domain here
    },
}

// export default nextConfig;
